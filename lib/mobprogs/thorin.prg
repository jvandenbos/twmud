>greet_prog 50
if ispc($n)
  if rand(50)
    if rand(50)
      raise
      look $n
      emote hefts his weapon in preparations for battle.
    else
      say Where is that Burglar?  We must be going shortly!
      scan
    endif
  else
    if rand(50)
      emote grumbles something under his breath.
    else
      say This key was left to me by my father before me.
      emote holds a small key up to the light.
      say I wonder what treasures it will lead us to...
    endif
  endif
endif
~
@

