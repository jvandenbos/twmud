int mana_gain(struct char_data* ch);
int hit_gain(struct char_data* ch);
int move_gain(struct char_data* ch);
void gain_condition(struct char_data* ch, int condition, int value);
void drain_life_pulse(int);
